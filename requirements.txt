# UNICC Council — core runtime dependencies (sandbox/CI safe)
# Heavy ML deps (chromadb, sentence-transformers, torch) are in requirements-full.txt
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
anthropic>=0.25.0
pyyaml>=6.0
httpx>=0.25.0
gitpython>=3.1.0
requests>=2.31.0
reportlab>=4.0.0
